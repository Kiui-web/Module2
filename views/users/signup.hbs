{{!-- <h2 class="mb-5 mt-5">New Account</h2>
<form action="/users" method="POST" enctype="multipart/form-data">
  <div class="form-group">
    <input name="name" type="text" class="form-control {{#if error.name }}is-invalid{{/if}}" id="name"
      placeholder="Enter name" value="{{ user.name }}">

    {{#if error.name}}
    <div class="invalid-feedback">
      {{error.name.message}}
    </div>
    {{/if}}
  </div>

  <div class="form-group">
    <input name="password" type="password" class="form-control {{#if error.password }}is-invalid{{/if}}" id="password"
      placeholder="Password" value="{{ user.password }}">

    {{#if error.password}}
    <div class="invalid-feedback">
      {{error.password.message}}
    </div>
    {{/if}}
  </div>

  <div class="form-group">
    <input name="email" type="email" class="form-control {{#if error.email }}is-invalid{{/if}}" id="email"
      placeholder="Enter email" value="{{ user.email }}">

    {{#if error.email}}
    <div class="invalid-feedback">
      {{error.email.message}}
    </div>
    {{/if}} --}}
<h1>Welcome to My Awesome App</h1>
<div id="firebaseui-auth-container"></div>
<div id="loader">Loading...</div>




<form action="/event" method="get">
  <input type="text" name="hola">
  <input type="submit" value="enviar">
</form>










{{!-- 
<div id="wrapper">
  <div id="dialog">
    <button class="close">×</button>
    <h3>Por favor introduce los 6 digitos de verificación que has recibido por SMS:</h3>
    <div id="form">
      <input type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
      <input type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
      <input type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
      <input type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
      <input type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
      <input type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" /><br>
      
    </div>
      <button id="confirm-code" class="btn btn-primary btn-embossed mt-3">Verificar</button>
    <div>
      No ha recibido el codigo?<br />
      <a href="#">Volver a enviar el código</a><br />
      <a href="/login">Cambia el número de telefono</a>
    </div>
  </div>
</div> --}}





      <div id="inset_form"></div>

    <!-- Add two inputs for "phoneNumber" and "code" -->
        <input type="tel" id="phoneNumber" />
        
        <input type="text" id="code" />
    
        <!-- Add two buttons to submit the inputs -->
        <button id="sign-in-button">
          SIGN IN WITH PHONE
        </button>
        <button id="confirm-code">
          ENTER CODE
        </button>

        <!-- Add a container for reCaptcha -->
        <div id="recaptcha-container"></div>
    
        <!-- Add the latest firebase dependecies from CDN -->
        <script src="https://www.gstatic.com/firebasejs/6.3.3/firebase-app.js"></script>
        <script src="https://www.gstatic.com/firebasejs/6.3.3/firebase-auth.js"></script>









  {{!-- </div>

    <div class="form-group">
    <input name="number" type="tel" class="form-control {{#if error.number }}is-invalid{{/if}}" id="number"
      placeholder="Enter number" value="{{ user.number }}">

    {{#if error.number}}
    <div class="invalid-feedback">
      {{error.number.message}}
    </div>
    {{/if}}
  </div> --}}

  {{!-- <button type="submit" class="btn btn-primary w-100">
    Sign up
  </button> --}}
</form>