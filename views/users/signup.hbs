{{!-- <h2 class="mb-5 mt-5">New Account</h2>
<form action="/users" method="POST" enctype="multipart/form-data">
  <div class="form-group">
    <input name="name" type="text" class="form-control {{#if error.name }}is-invalid{{/if}}" id="name"
      placeholder="Enter name" value="{{ user.name }}">

    {{#if error.name}}
    <div class="invalid-feedback">
      {{error.name.message}}
    </div>
    {{/if}}
  </div>

  <div class="form-group">
    <input name="password" type="password" class="form-control {{#if error.password }}is-invalid{{/if}}" id="password"
      placeholder="Password" value="{{ user.password }}">

    {{#if error.password}}
    <div class="invalid-feedback">
      {{error.password.message}}
    </div>
    {{/if}}
  </div>

  <div class="form-group">
    <input name="email" type="email" class="form-control {{#if error.email }}is-invalid{{/if}}" id="email"
      placeholder="Enter email" value="{{ user.email }}">

    {{#if error.email}}
    <div class="invalid-feedback">
      {{error.email.message}}
    </div>
    {{/if}} --}}

    <fieldset>
          <div>
            <label for="phoneNumber">Phone Number</label> <br>
            <input type="tel" id="phoneNumber" name="phoneNumber" /> <br>
          </div>
          <div>
            <label for="code">Code</label> <br>
            <input type="text" id="code" name="code"/> <br>
          </div>
          <div id="recaptcha-container"></div>
          <button id="signInWithPhone">Login</button>
          <button id="sentCodeId">Get Code</button>  
    </fieldset>
  {{!-- </div>

    <div class="form-group">
    <input name="number" type="tel" class="form-control {{#if error.number }}is-invalid{{/if}}" id="number"
      placeholder="Enter number" value="{{ user.number }}">

    {{#if error.number}}
    <div class="invalid-feedback">
      {{error.number.message}}
    </div>
    {{/if}}
  </div> --}}

  <button type="submit" class="btn btn-primary w-100">
    Sign up
  </button>
</form>