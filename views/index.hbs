<div class="container-fluid">
    <div class="row" id="grid-index">
    
        <div class="col-lg-6" id="grid-second">
        <div class="explain-index">
            <div class="text-left">
                <div class="explain-index-text"><i class="fa fa-plus-circle" aria-hidden="true"></i>Crea tus eventos personalizados.</div>
            
                <div class="explain-index-text"><i class="fa fa-share-alt" aria-hidden="true"></i>Comparte el evento a traves de whasap.</div>
            
                <div class="explain-index-text"><i class="fa fa-user-plus" aria-hidden="true"></i>Gestiona facilmente la asistencia.</div>
            </div>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="text-index" >
            <div class="text-left width-index">

                <img src="./images/kiui-logo.png" alt="kiui-logo" class="logo-index">

                <h1 class="title-index">
                    Organiza eventos con tus 
                        <span id="typewrite" class="wrap"></span>
                </h1>
                <p class="d-index">Gestionar eventos nunca ha sido tan fácil.</p>
                    <a href="/createEvent" class="btn btn-create">Crear evento</a><br>
                    <a href="/login" class="btn btn-login mt-3">Iniciar sesión</a>
           </div>
     </div>
    </div>
    </div>
</div>





<script>
    const name = ["amigos", "compañeros de trabajo", "colegas", "familiares"]
    let pos = 0

    const writing = (arr , pos = 0) => {
        let arrFromStr = arr[pos].split('');
        let span = document.getElementById('typewrite')
        let  i = 0;
        const total = arr.length - 1
        
        let printStr = setInterval(function(){
            span.innerHTML += arrFromStr[i];
            i++;

            if (i === arrFromStr.length ){
                clearInterval(printStr);
                notWriting(arr[pos], total)
            }
        },160);
    };
    

    let notWriting = (str, total) => {
        let span = document.getElementById('typewrite')
        let j = str.length
        total === pos ? pos = 0 : pos++
        
        const noprint = setInterval(function(){
             span.innerHTML = str.slice(0,j)
            j--
            if (j === -1 ){
                clearInterval(noprint);
                writing(name, pos)
            }
        },160);
    }

    writing(name);
</script>
